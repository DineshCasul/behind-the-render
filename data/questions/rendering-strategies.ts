import type { ConceptId, InterviewQuestion } from "@/lib/types";

export const renderingStrategiesQuestions: Partial<Record<ConceptId, InterviewQuestion[]>> = {
  csr: [
    {
      category: "fundamentals",
      question: "What does the server actually send on the first request of a pure CSR app?",
      answer: "A near-empty HTML shell plus script tags, no real content, since the UI is built entirely in the browser by JavaScript.",
      reasoning: "This is the defining trait of CSR: the server's job ends at serving static assets, not generating the page's content.",
      followUp: "What would a search crawler that doesn't execute JS see if it requested this same page?",
    },
    {
      category: "tricky",
      question: "Why doesn't CSR have a 'hydration' step the way SSR does?",
      answer: "Hydration means attaching behavior to DOM that already exists. In pure CSR there's no server-rendered DOM to attach to, React renders into the empty container from scratch.",
      reasoning: "Hydration is specifically the reconciliation-against-existing-DOM operation, without pre-existing DOM, there's nothing to reconcile against.",
      followUp: "What React API is used for this from-scratch render, and how does it differ from the hydration API?",
    },
    {
      category: "scenario",
      question: "A CSR dashboard app behind a login wall is criticized for 'bad SEO.' Is that criticism relevant?",
      answer: "Usually not: if all meaningful content requires authentication, search crawlers wouldn't be able to index it regardless of rendering strategy, so CSR's SEO weakness isn't actually costing anything here.",
      reasoning: "SEO tradeoffs only matter for content that's supposed to be publicly indexable, applying that critique universally, regardless of context, misses when a tradeoff actually applies.",
      followUp: "Would your answer change if the app had a public marketing site sharing the same codebase?",
    },
    {
      category: "senior",
      question: "You're migrating a CSR app to SSR purely for perceived performance. What could make the migration not actually help?",
      answer: "If the app's data fetching is slow or blocks the render, SSR just moves the same wait to the server, the response won't start until that fetch resolves, which can be just as slow (or slower under load) as the client fetching it itself.",
      reasoning: "SSR helps time-to-first-content when the server can render *fast*; it doesn't fix a fundamentally slow data dependency, it just relocates where the wait happens.",
      followUp: "How would streaming change this specific tradeoff?",
    },
    {
      category: "debugging",
      question: "A CSR app shows a blank white screen for several seconds on a slow connection before anything appears. What's happening, and what's one mitigation?",
      answer: "The browser is still downloading/parsing/executing the JS bundle (and possibly then fetching data) before React can render anything, nothing exists in the shell to show meanwhile. A loading skeleton in the initial HTML shell, or switching the page to SSR, are common mitigations.",
      reasoning: "CSR's time-to-first-content is gated by the *entire* JS download+execute+fetch chain, there's no partial content available before that chain completes.",
      followUp: "Why wouldn't simply making the JS bundle smaller fully solve this on a slow connection?",
    },
  ],
  ssr: [
    {
      category: "fundamentals",
      question: "What's the key difference between what the browser receives from SSR versus CSR on the first request?",
      answer: "SSR sends complete, content-filled HTML; CSR sends a near-empty shell that JavaScript fills in afterward.",
      reasoning: "This single difference is why SSR has faster perceived first content and better out-of-the-box crawler visibility.",
      followUp: "Does this difference persist after the page has fully loaded and hydrated?",
    },
    {
      category: "tricky",
      question: "Why doesn't SSR automatically make a page faster?",
      answer: "SSR improves *time to first visible content*, but total responsiveness also depends on server render time, data-fetch latency, and hydration time, a slow server or slow data source can make SSR slower overall than a well-cached static or CSR alternative.",
      reasoning: "\"Faster\" isn't one number: SSR trades a JS-download wait for a server-render wait, which is only a net win if the server side is actually quicker.",
      example: "A slow third-party API called during SSR blocks the entire response, even though the same call in CSR would only block one section of an already-visible page.",
      followUp: "What technique addresses exactly this kind of slow-data-blocks-everything problem within SSR?",
    },
    {
      category: "scenario",
      question: "An SSR page looks fully loaded but clicking buttons does nothing for a noticeable moment. What's going on?",
      answer: "The HTML rendered and painted before the hydration JavaScript finished downloading/executing, the page is visible but not yet interactive.",
      reasoning: "SSR intentionally decouples 'visible' from 'interactive'; the gap between them is exactly the hydration window.",
      followUp: "What would reducing the JS bundle size (e.g. via Server Components) do to this gap?",
    },
    {
      category: "senior",
      question: "When would you choose SSR over SSG for a page whose content rarely changes?",
      answer: "When the page needs genuinely request-specific data even though the base content is stable, e.g. embedding a per-user greeting or A/B test variant directly in the initial HTML, which a build-time-only SSG render can't do.",
      reasoning: "The rarity of content changes argues for SSG's caching benefits, but per-request personalization is a hard requirement SSG structurally can't satisfy without a client-side patch afterward.",
      followUp: "Could ISR plus client-side personalization achieve a similar result with less server cost?",
    },
    {
      category: "debugging",
      question: "Production SSR pages intermittently return a 500 error under load, but the app works fine with low traffic. What's a likely culprit?",
      answer: "Per-request render cost multiplying under concurrency, e.g. a slow synchronous render blocking the server's event loop/worker, or a downstream data source (database, API) that can't handle the concurrent query volume SSR generates per visitor.",
      reasoning: "SSR trades static-serving's near-zero per-request cost for real per-request computation, that cost scales with traffic in a way static/cached responses don't.",
      followUp: "How would caching or moving to ISR reduce this specific failure mode?",
    },
  ],
  ssg: [
    {
      category: "fundamentals",
      question: "When does rendering actually happen for a statically generated page?",
      answer: "Once, at build time: not per request.",
      reasoning: "This is the entire point of SSG: do the render work exactly once and reuse the output indefinitely.",
      followUp: "What has to happen for a visitor to see updated content after the underlying data changes?",
    },
    {
      category: "tricky",
      question: "Can a statically generated page still have dynamic, per-user behavior?",
      answer: "Yes: the initial HTML is static, but client-side JavaScript running after load can fetch fresh data, read cookies/localStorage, and update the UI dynamically. SSG constrains how the *initial markup* was produced, not what the page can do afterward.",
      reasoning: "Conflating 'the HTML is static' with 'the page can't be dynamic' is one of the most common SSG misconceptions.",
      followUp: "Where would you put logic that needs to show different content per logged-in user, given the HTML itself can't vary per visitor?",
    },
    {
      category: "scenario",
      question: "A marketing site with 50,000 statically generated product pages has build times exceeding 40 minutes. What are your options?",
      answer: "Reduce what's pre-rendered at build time (generate only the most-trafficked pages statically, render the rest on-demand), or switch those pages to ISR so individual pages can be generated/refreshed independently instead of all at once during a full build.",
      reasoning: "SSG's per-page cost is fixed, but it's multiplied by every page in the build, at large scale that fixed cost stops being negligible, which is exactly the problem ISR (and on-demand generation) is designed to solve.",
      followUp: "How does on-demand ISR generation differ from generating everything upfront at build time?",
    },
    {
      category: "senior",
      question: "Why might build-time rendering catch bugs that would otherwise only surface in production under SSR?",
      answer: "Because every page is actually rendered during the build, a rendering error (a bad prop, a null data field) surfaces immediately as a failed build, under SSR, the same bug might only trigger for a specific request's data at runtime, in production.",
      reasoning: "SSG effectively runs the render for every known input ahead of time, which is a form of exhaustive testing SSR's per-request, lazy execution doesn't provide.",
      followUp: "What kind of bug would SSG's build-time check *not* catch, that SSR could still hit at runtime?",
    },
    {
      category: "debugging",
      question: "You fixed a typo in a statically generated page's content, redeployed, but users still see the old text. What's the most likely cause?",
      answer: "A CDN or browser cache still serving the previously built static file, the new build produced correct output, but a caching layer between the origin and the user hasn't picked up the change yet.",
      reasoning: "SSG output being cacheable is a feature, but it also means a stale cache can outlive a successful deploy if invalidation wasn't triggered.",
      followUp: "What would you check to confirm this is a caching issue rather than a build issue?",
    },
  ],
  isr: [
    {
      category: "fundamentals",
      question: "What does a visitor receive when they request an ISR page whose revalidation window has expired?",
      answer: "The existing (now stale) static file, immediately, with no wait for regeneration. The rebuild happens in the background for the next visitor.",
      reasoning: "This 'stale-while-revalidate' behavior is ISR's defining property: a visitor is not blocked while an existing page regenerates.",
      followUp: "What do subsequent visitors receive after that background regeneration finishes?",
    },
    {
      category: "tricky",
      question: "Is ISR the same thing as 'SSR with caching'?",
      answer: "No: SSR-with-caching would still risk a slow or failed render blocking a request on a cache miss. ISR's guarantee is that a request is not blocked while an existing page regenerates: it serves the existing static response immediately, stale or not. (A page that was never generated is built on its first request, so that visitor does wait.)",
      reasoning: "The distinction is about failure/latency modes: a naive SSR cache can still degrade to an uncached, slow render under a cache miss; ISR structurally avoids that by always having *something* pre-built to serve.",
      followUp: "What happens under ISR if the very first request for a page arrives before it's ever been generated at all?",
    },
    {
      category: "scenario",
      question: "An e-commerce site needs product prices to update within a minute of a backend change, across thousands of product pages. Would you reach for SSG, SSR, or ISR, and why?",
      answer: "ISR: SSG alone can't refresh without a full rebuild, and SSR would re-render every product page on every single request even though prices only actually change occasionally.",
      reasoning: "ISR is specifically suited to 'mostly static, occasionally changes, at scale', exactly this shape of problem.",
      followUp: "Would on-demand revalidation (triggered by the price-change event) be a better fit here than a fixed time-based window?",
    },
    {
      category: "senior",
      question: "What operational risk does time-based ISR revalidation introduce at scale that on-demand revalidation avoids?",
      answer: "If many pages share the same revalidation window, they can all go stale simultaneously and trigger a burst of regeneration work at once, spiking server load, on-demand revalidation only regenerates specific pages exactly when their underlying data actually changes.",
      reasoning: "Time-based windows couple regeneration timing to a clock, not to actual data change events, which can create load patterns disconnected from real need.",
      followUp: "How would you stagger revalidation windows across pages to avoid this 'thundering herd' effect?",
    },
    {
      category: "debugging",
      question: "A page under ISR shows stale content well beyond its configured revalidation window. What would you investigate?",
      answer: "Whether background regeneration is actually succeeding (check server logs for errors during regeneration), whether a CDN in front of the origin is caching the response independently and ignoring the framework's revalidation signal, and whether the hosting platform actually supports ISR's on-demand regeneration model.",
      reasoning: "ISR depends on the hosting infrastructure correctly implementing per-page background regeneration, a generic CDN cache layered on top without ISR-awareness can silently override it.",
      followUp: "How would you distinguish 'regeneration is failing silently' from 'a CDN cache is masking a successful regeneration'?",
    },
  ],
};
